#!/usr/bin/env node
'use strict';

const fs=require('fs');
const path=require('path');
const{bootRenderedGame,encodeRgbaPng}=require('../render/runtime');
const{
  sha256,analyzeFrame,frameDifference,structureDistance,analyzeBurst,measureDrawnActorExtent,
  assertActorScale,writeContactSheet,verifyReviewReceipt,writeJson,quantile,manifestSha256,
  createEvidenceManifest,createDependencyManifest,writeVisualProvenance,AMBIENT_EVIDENCE_PROTOCOL
}=require('./visual-harness');
const{runFeedbackVisibility,assertFeedback,feedbackLine}=require('./feedback');

const ROOT=path.join(__dirname,'..'),GAME='lantern-line',GAME_PATH=path.join(ROOT,GAME+'.html');
const ARTIFACT_DIR=path.join(ROOT,'.artifacts','visual',GAME),FRAME_DIR=path.join(ARTIFACT_DIR,'frames');
const CONTACT_PATH=path.join(ARTIFACT_DIR,'contact-sheet.png'),METRICS_PATH=path.join(ARTIFACT_DIR,'metrics.json'),TEMPLATE_PATH=path.join(ARTIFACT_DIR,'review-template.json'),PROVENANCE_PATH=path.join(ARTIFACT_DIR,'visual-provenance.json');
const REVIEW_PATH=path.join(__dirname,'visual-reviews',GAME+'.json'),PRESERVED_PATH=path.join(__dirname,'visual-receipts',GAME+'-contact-sheet.png'),CLIP_PATH=path.join(ARTIFACT_DIR,GAME+'-30s.mp4');
const SEED=0x1a47,RENDER_EVERY=2,WORLD_CROP={x:0,y:0,width:160,height:310};
const RENDER_COMMAND='node render/render.js lantern-line 30 .artifacts/visual/lantern-line/lantern-line-30s.mp4 --seed 0x1a47 --probe --fps 30';
const median=values=>quantile(values,.5),GAME_SOURCE=fs.readFileSync(GAME_PATH,'utf8');

function probe(runtime){const fn=runtime.sandbox.__lanternLineVisualProbe;if(typeof fn!=='function')throw new Error('missing __lanternLineVisualProbe');const value=fn();if(!value||value.finite===false)throw new Error('non-finite Lantern Line visual fixture');return value}
function captureFixture(name,offsets,options){options=options||{};const runtime=bootRenderedGame(GAME,{seed:SEED});if(options.beforeSet)options.beforeSet(runtime);const set=runtime.sandbox.__lanternLineSetVisualBeat;if(typeof set!=='function'||!set(name))throw new Error('unknown visual beat '+name);if(options.selector!==undefined)runtime.sandbox.__LL_VISUAL_ONLY_SUBJECT=options.selector;if(options.afterSet)options.afterSet(runtime);const frames=new Map();for(const target of[...new Set(offsets)].sort((a,b)=>a-b)){runtime.advanceTo(target,{renderEvery:RENDER_EVERY,renderLast:true});const frame=runtime.snapshot({native:true});frame.probe=probe(runtime);frame.fixture=name;frame.offset=target;frames.set(target,frame)}return frames}
function captureTimeline(game,seed,targets){const runtime=bootRenderedGame(game,{seed}),frames=new Map();for(const target of targets){if(target-runtime.frame>120)runtime.advanceTo(target-120);runtime.advanceTo(target,{renderEvery:RENDER_EVERY,renderLast:true});frames.set(target,runtime.snapshot({native:true}))}return frames}
function limits(actor){if(actor.kind==='keeper')return{maxWidth:18,maxHeight:27,label:'lightkeeper'};if(actor.kind==='sootling')return{maxWidth:18,maxHeight:19,label:'sootling'};return{maxWidth:16,maxHeight:16,label:'mote'}}
function measureActors(fixture,offset){const base=captureFixture(fixture,[offset],{selector:'none'}).get(offset),out=[];for(const actor of base.probe.actors){const isolated=captureFixture(fixture,[offset],{selector:actor.id}).get(offset),measurement=measureDrawnActorExtent(isolated,base,{id:actor.id,kind:actor.kind,type:actor.type,probeBox:actor.box,padding:8,threshold:8}),assertion=assertActorScale(measurement,limits(actor));out.push({...measurement,assertion:{ok:assertion.ok,failures:assertion.failures,limits:assertion.limits}})}return{probe:base.probe,measurements:out}}
function feedbackEvidence(){const options={frames:9000,signatureProbe:'__lanternLineSignature',poll:4,radius:28,perCategory:3,goodPalette:['#79f2d0','#ffc857','#58e5e8'],badPalette:['#ff665f','#ff6fae'],paletteTolerance:62};return[0x1a41,0x1ae7].map(seed=>runFeedbackVisibility(GAME,{...options,seed}))}
function reviewTemplate(montageHash,gameHash,beats,evidenceManifest,dependencyManifest,identity){const pending=note=>({meetsMachineHunt:false,meetsBlockMine:false,note});return{schema:2,protocol:AMBIENT_EVIDENCE_PROTOCOL,game:GAME,verdict:'pending',references:['horizon','blockmine'],montageSha256:montageHash,gameSha256:gameHash,reviewIdentitySha256:identity,evidenceManifest,evidenceManifestSha256:manifestSha256(evidenceManifest),dependencyManifest,dependencyManifestSha256:manifestSha256(dependencyManifest),seed:'0x'+SEED.toString(16),checkpoints:beats.map(beat=>beat.id+'@'+beat.offset),reviewedAt:'YYYY-MM-DD',reviewer:'PENDING native-size review',categories:{characterCraft:pending('Inspect the lightkeeper, sootling silhouettes, working poses, motes, and visible state changes at native 160x360.'),environmentCraft:pending('Inspect the three independent lane panels, prisms, coils, fusers, rework and purge response stations, ducts, skyline, and material separation.'),levelVariety:pending('Confirm order, sabotage, repair, launch, district, and beacon beats materially change state and composition rather than only changing copy.'),animationImpact:pending('Confirm moving motes, keeper station work, sootling scout/windup/clog motion, machine response, exact launch, district light, and beacon payoff have anticipation and follow-through.'),readability:pending('Confirm two-part recipes, lane machinery, sootling target tell, keeper reaction, jam state, exact payoff, permanent district progress, and final beacon read without a computed route overlay.'),artDirectionCohesion:pending('Confirm the stained-glass color system, brass/steel workshop, soot threat grammar, skyline progression, HUD, and good/bad feedback feel authored as one world.')},renderReceipt:{seed:'0x'+SEED.toString(16),seconds:30,fps:30,codec:'h264',dimensions:'320x720',bytes:0,sha256:'',command:RENDER_COMMAND}}}

async function main(){
  fs.mkdirSync(FRAME_DIR,{recursive:true});for(const file of fs.readdirSync(FRAME_DIR))if(file.endsWith('.png'))fs.unlinkSync(path.join(FRAME_DIR,file));
  const specs=[
    {id:'order',label:'order read',fixture:'order-read',offset:8,burst:[1,4,8,12]},
    {id:'route',label:'route choice',fixture:'route-choice',offset:8,burst:[1,4,8,12]},
    {id:'scout',label:'sootling scout',fixture:'sootling-scout',offset:8,burst:[1,4,8,12]},
    {id:'warning',label:'clog warning',fixture:'clog-warning',offset:8,burst:[1,4,8,12]},
    {id:'reroute',label:'keeper response',fixture:'keeper-reroute',offset:12,burst:[1,4,8,12]},
    {id:'jam',label:'jam impact',fixture:'jam-impact',offset:6,burst:[1,3,6,10]},
    {id:'imperfect',label:'dim lantern',fixture:'imperfect-lantern',offset:6,burst:[1,3,6,12]},
    {id:'exact',label:'exact lantern',fixture:'exact-lantern',offset:6,burst:[1,3,6,12]},
    {id:'district',label:'district lit',fixture:'district-lit',offset:6,burst:[1,3,6,12]},
    {id:'apex',label:'beacon apex',fixture:'beacon-apex',offset:6,burst:[1,3,6,12]}
  ];
  const frames={},runs={},repeat={};for(const spec of specs){runs[spec.id]=captureFixture(spec.fixture,spec.burst);repeat[spec.id]=captureFixture(spec.fixture,[spec.offset]);frames[spec.id]=runs[spec.id].get(spec.offset);fs.writeFileSync(path.join(FRAME_DIR,String(specs.indexOf(spec)+1).padStart(2,'0')+'-'+spec.id+'.png'),encodeRgbaPng(frames[spec.id]))}
  const deterministic=specs.map(spec=>({id:spec.id,first:sha256(frames[spec.id].rgba),second:sha256(repeat[spec.id].get(spec.offset).rgba),ok:sha256(frames[spec.id].rgba)===sha256(repeat[spec.id].get(spec.offset).rgba)}));
  const refTargets=[60,600,1200,2400,3600,5400,7200,9000,12000,14400],horizon=captureTimeline('horizon',0xa1020401,refTargets),blockmine=captureTimeline('blockmine',0xb10c0050,refTargets),horizonFrames={},blockmineFrames={};specs.forEach((spec,index)=>{horizonFrames[spec.id]=horizon.get(refTargets[index]);blockmineFrames[spec.id]=blockmine.get(refTargets[index])});
  const sheet=writeContactSheet({beats:specs,rows:[{label:'LANTERN LINE',frames},{label:'MACHINE HUNT',frames:horizonFrames},{label:'BLOCK MINE',frames:blockmineFrames}],outPath:CONTACT_PATH});
  const candidate=Object.fromEntries(specs.map(spec=>[spec.id,analyzeFrame(frames[spec.id],{native:false,crop:WORLD_CROP})])),cm=Object.values(candidate),hm=specs.map(spec=>analyzeFrame(horizonFrames[spec.id],{native:false,crop:WORLD_CROP})),bm=specs.map(spec=>analyzeFrame(blockmineFrames[spec.id],{native:false,crop:WORLD_CROP})),refEdge=Math.min(median(hm.map(m=>m.edge[1].energy)),median(bm.map(m=>m.edge[1].energy))),refRich=Math.min(median(hm.map(m=>m.richCellFraction)),median(bm.map(m=>m.richCellFraction)));
  const scale=measureActors('order-read',8),playfield=scale.probe.layout.playfield,footprint=scale.measurements.reduce((sum,m)=>sum+m.bboxArea,0)/(playfield.width*playfield.height);
  const warningDelta=frameDifference(frames.warning,frames.jam,{native:false,crop:WORLD_CROP}),imperfectDelta=frameDifference(frames.jam,frames.imperfect,{native:false,crop:WORLD_CROP}),districtBase=captureFixture('order-read',[6],{selector:'none'}).get(6),districtNoActors=captureFixture('district-lit',[6],{selector:'none',beforeSet:r=>{r.sandbox.__NO_PAYOFF_FX=1}}).get(6),apexNoActors=captureFixture('beacon-apex',[6],{selector:'none',beforeSet:r=>{r.sandbox.__NO_PAYOFF_FX=1}}).get(6),districtDelta=frameDifference(districtBase,districtNoActors,{native:false,crop:{x:0,y:0,width:160,height:60}}),apexStructure=structureDistance(districtNoActors,apexNoActors,{crop:{x:0,y:0,width:160,height:60}});
  const clean=captureFixture('route-choice',[1]).get(1),dirty=captureFixture('route-choice',[1],{afterSet:r=>r.sandbox.__lanternLinePlannerContamination()}).get(1),plannerHashes={clean:sha256(clean.rgba),contaminated:sha256(dirty.rgba)};
  const apexNoFx=captureFixture('beacon-apex',[6],{beforeSet:r=>{r.sandbox.__NO_PAYOFF_FX=1}}).get(6),apexFx=frameDifference(apexNoFx,frames.apex,{native:false,crop:WORLD_CROP}),bursts=Object.fromEntries(specs.map(spec=>[spec.id,analyzeBurst(spec.burst.map(offset=>runs[spec.id].get(offset)),{native:false,crop:WORLD_CROP})]));
  const feedbackRuns=feedbackEvidence(),feedbackFailures=[],feedbackSummary=assertFeedback(GAME,feedbackRuns,{required:['good:transform','good:exact-lantern','good:district-lit','good:purge-capture','bad:jam','bad:wrong-transform'],minChanged:{default:20},minSignature:{default:5},maxInvisible:0},message=>feedbackFailures.push(message));
  const bands={colors:88,entropy:2.62,luma:.125,largest:.49,edge:.0203,richEach:.72,richMedian:.76,footprint:.05,warningChanged:.065,warningGrid:.25,districtChanged:.006,apexStructure:.08,burst:.01,apexFx:.006,apexFxGrid:.12};
  const gates=[],gate=(name,ok,detail)=>gates.push({name,ok:!!ok,detail});gate('same-seed native pixels are deterministic',deterministic.every(item=>item.ok),deterministic);gate('all authored checkpoints are finite and truthful',specs.every(spec=>frames[spec.id].probe&&frames[spec.id].probe.finite&&frames[spec.id].probe.fixture===spec.fixture),specs.map(spec=>frames[spec.id].probe));gate('lightkeeper, sootling, and mote drawn extents stay at native reference scale',scale.measurements.some(m=>m.kind==='keeper')&&scale.measurements.some(m=>m.kind==='sootling')&&scale.measurements.some(m=>m.kind==='mote')&&scale.measurements.every(m=>m.assertion.ok),scale.measurements);gate('watched actors occupy less than five percent of the workshop',footprint<=bands.footprint,{footprint,measurements:scale.measurements});gate('frames remain opaque, rich, contrasted, and non-flat',cm.every(m=>m.opaqueFraction===1&&m.quantizedColors>=bands.colors&&m.colorEntropy>=bands.entropy&&m.lumaStdDev>=bands.luma&&m.largestColorShare<=bands.largest),cm);gate('multiscale optical detail is reference-comparable',cm.every(m=>m.edge[1].energy>=bands.edge&&m.edge[4].energy>m.edge[1].energy)&&median(cm.map(m=>m.edge[1].energy))>=refEdge*.72,{candidate:cm.map(m=>m.edge),referenceFloor:refEdge});gate('spatial richness fills the native workshop strip',cm.every(m=>m.richCellFraction>=bands.richEach)&&median(cm.map(m=>m.richCellFraction))>=Math.max(bands.richMedian,refRich*.84),{candidate:cm.map(m=>m.richCellFraction),referenceFloor:refRich});
  const forbidden={routeRenderer:/\bfunction\s+draw(?:Route|Path)s?\b|\bdrawRoutes?\s*\(/.test(GAME_SOURCE),routeProbe:/\broute(?:Points|Hash)\s*:/.test(GAME_SOURCE),guidelineDash:/\.setLineDash\s*\(/.test(GAME_SOURCE),visiblePlanCopy:/VISIBLE (?:ROUTE )?PLAN|FOLLOW THE (?:PATH|LINE)|CHECK THE PLAN/.test(GAME_SOURCE)};gate('computed future routes have no presentation surface',Object.values(forbidden).every(value=>!value)&&specs.every(spec=>frames[spec.id].probe.guidelines===0),forbidden);gate('contaminating private future-route data is an exact pixel no-op',plannerHashes.clean===plannerHashes.contaminated,plannerHashes);gate('sootling warning and physical jam impact are visually distinct',warningDelta.changedFraction>=bands.warningChanged&&warningDelta.changedGridFraction>=bands.warningGrid,warningDelta);gate('misfire resolves into a visibly distinct dim-lantern aftermath',imperfectDelta.changedFraction>=.04&&imperfectDelta.changedGridFraction>=.2,imperfectDelta);gate('district light and beacon permanently change the skyline',districtDelta.changedFraction>=bands.districtChanged&&apexStructure.structureDistance>=bands.apexStructure,{districtDelta,apexStructure});gate('keeper, sootling, machinery, launch, and beacon beats animate',bursts.route.changedFraction.max>=bands.burst&&bursts.scout.changedFraction.max>=bands.burst&&bursts.warning.changedFraction.max>=bands.burst&&bursts.exact.changedFraction.max>=bands.burst&&bursts.apex.changedFraction.max>=bands.burst,bursts);gate('beacon apex adds authored payoff pixels',apexFx.changedFraction>=bands.apexFx&&apexFx.changedGridFraction>=bands.apexFxGrid,apexFx);gate('good and bad beats are locally visible and palette-separated',feedbackFailures.length===0,{summary:feedbackLine(feedbackRuns),feedbackSummary,failures:feedbackFailures,runs:feedbackRuns});
  const checkpoints=specs.map(spec=>spec.id+'@'+spec.offset),evidenceManifest=createEvidenceManifest(GAME,{checkpoints,fixtures:Object.fromEntries(specs.map(spec=>[spec.id,{fixture:spec.fixture,offset:spec.offset,burst:spec.burst}])),naturalSequence:{seeds:feedbackRuns.map(run=>run.seed),frames:9000,contract:'good/bad real-pixel lockstep'},probeFields:['finite','fixture','actors','modules','districts','guidelines','layout.playfield'],crops:{world:WORLD_CROP,skyline:{x:0,y:0,width:160,height:60},feedbackRadius:28},actors:{selector:'__LL_VISUAL_ONLY_SUBJECT',scaleCaps:{keeper:[18,27],sootling:[18,19],mote:[16,16]},maxFootprint:.05},metrics:{bands,feedbackRequired:['good:transform','good:exact-lantern','good:district-lit','good:purge-capture','bad:jam','bad:wrong-transform'],plannerContamination:'exact-pixel-no-op'},references:['horizon','blockmine']}),dependencyManifest=createDependencyManifest(GAME,{root:ROOT,visualEvalPath:__filename,references:['horizon','blockmine']}),provenance=writeVisualProvenance(PROVENANCE_PATH,GAME,{root:ROOT,visualEvalPath:__filename,references:['horizon','blockmine'],montageSha256:sheet.sha256,evidenceManifest,dependencyManifest}),gameHash=sha256(GAME_PATH);writeJson(TEMPLATE_PATH,reviewTemplate(sheet.sha256,gameHash,specs,evidenceManifest,dependencyManifest,provenance.reviewIdentitySha256));
  let review;if(fs.existsSync(REVIEW_PATH)){review=verifyReviewReceipt(REVIEW_PATH,{root:ROOT,game:GAME,montageSha256:sheet.sha256,preservedPath:PRESERVED_PATH,visualEvalPath:__filename,evidenceManifest,provenance});if(review.receipt.gameSha256!==gameHash||review.receipt.seed!=='0x'+SEED.toString(16)){review.ok=false;review.errors.push('review game hash or seed is stale')}}else review={ok:false,errors:['missing semantic review '+REVIEW_PATH,'inspect '+CONTACT_PATH+' and complete '+TEMPLATE_PATH]};gate('fresh native-size semantic comparison receipt',review.ok,review.errors);
  const reviewClip=review.receipt&&review.receipt.renderReceipt,localClip=fs.existsSync(CLIP_PATH)?{path:CLIP_PATH,bytes:fs.statSync(CLIP_PATH).size,sha256:sha256(CLIP_PATH)}:null;gate('rendered autoplay clip receipt is complete',!!reviewClip&&reviewClip.bytes>100000&&/^[a-f0-9]{64}$/.test(reviewClip.sha256||'')&&reviewClip.command===RENDER_COMMAND&&reviewClip.seed==='0x'+SEED.toString(16),reviewClip);gate('local rendered clip matches receipt when available',!localClip||!!reviewClip&&localClip.bytes===reviewClip.bytes&&localClip.sha256===reviewClip.sha256,{localClip,reviewClip});
  const nonReview=new Set(['fresh native-size semantic comparison receipt','rendered autoplay clip receipt is complete','local rendered clip matches receipt when available']),report={schema:2,protocol:AMBIENT_EVIDENCE_PROTOCOL,game:GAME,gameSha256:gameHash,seed:'0x'+SEED.toString(16),contactSheet:{path:CONTACT_PATH,trackedPath:PRESERVED_PATH,sha256:sheet.sha256,width:sheet.width,height:sheet.height},provenance:{path:PROVENANCE_PATH,reviewIdentitySha256:provenance.reviewIdentitySha256,evidenceManifestSha256:provenance.evidenceManifestSha256,dependencyManifestSha256:provenance.dependencyManifestSha256},checkpoints:Object.fromEntries(specs.map(spec=>[spec.id,{fixture:spec.fixture,offset:spec.offset,probe:frames[spec.id].probe}])),thresholds:{bands,referenceEdge:refEdge,referenceRich:refRich},clip:localClip,metrics:{candidate,horizon:hm,blockmine:bm,scale:scale.measurements,footprint,plannerHashes,warningDelta,imperfectDelta,districtDelta,apexStructure,bursts,apexFx,feedbackRuns},gates,automatedOk:gates.filter(g=>!nonReview.has(g.name)).every(g=>g.ok),semanticReview:{path:REVIEW_PATH,ok:review.ok,errors:review.errors}};writeJson(METRICS_PATH,report);
  console.log('LANTERN LINE visual evidence · seed 0x'+SEED.toString(16));for(const item of gates)console.log('  '+(item.ok?'PASS':'FAIL')+' '+item.name);console.log('  '+feedbackLine(feedbackRuns));console.log('  contact: '+CONTACT_PATH);console.log('  montage sha256: '+sheet.sha256);console.log('  metrics: '+METRICS_PATH);console.log('  review template: '+TEMPLATE_PATH);if(!gates.every(item=>item.ok)){console.error('\nLANTERN LINE VISUAL EVAL FAILED');process.exit(1)}console.log('\nLANTERN LINE VISUAL EVAL PASSED');
}
main().catch(error=>{console.error('LANTERN LINE VISUAL EVAL FAILED:',error.stack||error);process.exit(1)});
